<UserControl x:Class="PointOfSale.Screens.Menus.ItemCustomization"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale.Screens.Menus"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Name="customizeItemLabel" TextAlignment="Center" FontSize="25" FontWeight="Bold">Customize Briarheart Burger</TextBlock>

        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Name="cancelButton" FontSize="20" Margin="10" Click="CancelClicked">Cancel</Button>
            <Button Grid.Row="0" Grid.Column="1" Name="finishedButton" FontSize="20" Margin="10" Click="AddItemClicked">Add Item</Button>
        </Grid>
        
        <Border Name="customizationContainer" BorderBrush="Silver" BorderThickness="1" CornerRadius="8" Margin="5">
        </Border>
    </DockPanel>
</UserControl>
